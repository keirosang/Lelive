/**
 * Created by Volio on 2016/3/20.
 */
cover.onchange = function () {
    if(!this.files||!this.files[0])
        return alert('选取文件出错！')

    var imagefile=this.files[0]

    if(imagefile.type.indexOf('image')!=0)
        return alert('这不是一个图片！')

    coverinfo.innerHTML = '已选择图片，点击上传'
}